// global.slint - 全局数据契约与主题配色

export global Theme {
    out property <color> bg-primary: #0f172a;
    out property <color> bg-card: #1e293b;
    out property <color> bg-hover: #334155;
    out property <color> border-color: #334155;

    out property <color> text-primary: #f8fafc;
    out property <color> text-secondary: #94a3b8;
    out property <color> text-muted: #64748b;

    out property <color> success: #10b981;
    out property <color> success-bg: #10b98133;
    out property <color> warning: #f59e0b;
    out property <color> warning-bg: #f59e0b33;
    out property <color> danger: #ef4444;
    out property <color> danger-bg: #ef444433;
    out property <color> info: #3b82f6;
    out property <color> info-bg: #3b82f633;
    out property <color> junction: #8b5cf6;
    out property <color> junction-bg: #8b5cf633;

    out property <color> progress-start: #3b82f6;
    out property <color> progress-end: #2563eb;
}

export enum DirectoryRating {
    Safe,
    Warning,
    Forbidden
}

export enum MigrationStatus {
    Idle,
    Scanning,
    Initiated,
    Copying,
    Validating,
    Linking,
    Completed,
    RollingBack,
    Error
}

export struct TreeNode {
    id: int,
    path: string,
    name: string,
    size-text: string,
    actual-size-bytes: int,
    rating: DirectoryRating,
    level: int,              // 层级深度 0=根, 1=子, 2=孙...
    is-expanded: bool,
    has-children: bool,
    is-selected: bool,
    is-junction: bool,       // 是否已建立目录联接
    is-visible: bool,        // 父级是否展开决定可见性
    children-count: int,
}

export struct LogEntry {
    timestamp: string,
    level: string,           // "info", "warn", "error", "success"
    message: string,
}
